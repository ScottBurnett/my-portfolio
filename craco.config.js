module.exports = {
  style: {
    postcss: {
      plugins: [
        require('tailwindcss'),
        require('autoprefixer'),
        require('tailwind'),
        require('react'),
        requestIdleCallback
        fetch return;
      ],
    },
  },
}